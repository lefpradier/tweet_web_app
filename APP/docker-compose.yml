version: '2'

services:
  frontend:
    build: frontend
    ports:
      - 8501:8501
    depends_on:
      - backend
    volumes:
        - ./APP:/home/lpradier/Bureau/FORMATION/PROJET_7/deployment/APP
  backend:
    build: backend
    ports:
      - 8080:8080
    volumes:
      - ./APP:/home/lpradier/Bureau/FORMATION/PROJET_7/deployment/APP
